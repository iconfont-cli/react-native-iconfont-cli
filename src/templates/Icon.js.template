/* eslint-disable */

import React from 'react';
#svgComponents#
#imports#

#comments#
const Icon = ({ color, name, size, ...rest }) => {
  switch (name) {
#cases#
  }

  return null;
};

Icon.defaultProps = {
  size: #size#,
};
#colorFunc#
export default Icon;
